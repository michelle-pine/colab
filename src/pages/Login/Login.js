import React from 'react';
import PropTypes from 'prop-types';
import './Login.scss';

const Login = props => {
  return (
    <div>
    Login
    </div>
  );
};

Login.defaultProps = {

};

Login.propTypes = {

};

export default Login;
